# Core ML
torch>=2.0.0
torchvision>=0.15.0
numpy>=1.24.0
einops>=0.6.1

# Diffusion Policy (upstream library — backbone & env wrappers)
# Install manually:  pip install git+https://github.com/real-stanford/diffusion_policy.git
# or clone and pip install -e .

# Diffusers (DDPM / EDM schedulers)
diffusers>=0.24.0

# Data
h5py>=3.8.0
robosuite>=1.4.0    # Robomimic simulation environments
robomimic>=0.3.0

# Config management  (matches diffusion_policy's tooling)
hydra-core>=1.3.0
omegaconf>=2.3.0

# Logging / evaluation
tqdm>=4.65.0
wandb>=0.15.0
imageio>=2.28.0
opencv-python>=4.7.0
